ExampleCanvas6:

#:set s dp(150)

<ExampleCanvas4>:
    Button:
        text: "Push"
        size: s/2, s/2
        on_press: root.on_click_action()
        pos: 0, 300

<ExampleGridLayout@GridLayout>:
    cols: 3
    canvas:
        Rectangle:
            pos: self.center_x-s/2, self.center_y-s/2
            size: s, s
        Line:
            # points: (0, 0, 100, 100, 200, 0, 300, 100)
            points: (0,0,self.width/4,100,self.width/2, 0, self.width*3/4, 100, self.width, 0)
            width: 2
        Ellipse:
            pos: self.center_x-s*1.5, self.center_y-s/2
            size: s, s/2

<ExampleGridLayout2@GridLayout>:
    canvas:
        Line:
            points: (0, self.height/2, self.width, self.height/2)
            width: dp(2)
        Line:
            points: (self.width/2, 0, self.width/2, self.height)
            width: 2

<ExampleWidget3@Widget>:
    canvas:
        Color:
            rgb: 1,0, 0
        Line:
            circle: (200, 100, 50) # center_x, center_y, radius
        Line:
            ellipse: (100, 200, 100, 50) # x, y, radius_x, radius_y
        Line:
            rectangle: (300, 150, 200, 80) # x, y, w, h

<ExampleCanvas6@BoxLayout>:
    orientation: 'vertical'
    size: root.size
    RelativeLayout:
        canvas:
            Color:
                rgb: 0, 0.63, 0.87
            Rectangle:
                size: self.size
    BoxLayout:
        orientation: 'vertical'
        RelativeLayout:
            canvas:
                Color:
                    rgb: .98, .82, .004
                Rectangle:
                    size: self.size
        RelativeLayout:
            canvas:
                Color:
                    rgb: .125, .38, .24
                Rectangle:
                    size: self.size
            
    